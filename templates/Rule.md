# Rule Template

Use `schemas/business-rule.schema.json` or `schemas/validation-rule.schema.json`.

Every rule declares authority, owner, Workspace scope, typed inputs, applicability, deterministic decision, outputs, priority, effective period, explanation, conflicts, and tests.

