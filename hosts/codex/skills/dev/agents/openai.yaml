interface:
  display_name: "Developer QA Gate"
  short_description: "Validate a change before release"
  default_prompt: "Use $dev to derive observable acceptance criteria and test this change."
dependencies:
  tools:
    - type: "mcp"
      value: "qa-intelligence"
      description: "QA Intelligence execution and evidence server"
policy:
  allow_implicit_invocation: true
