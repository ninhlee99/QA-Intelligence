# Schema Template

Every schema SHALL define `$schema`, stable `$id`, title, type, required fields, property constraints, compatibility policy, examples, governing ontology/specification, and negative validation cases.

Schemas define representation and SHALL NOT invent semantic meaning.

