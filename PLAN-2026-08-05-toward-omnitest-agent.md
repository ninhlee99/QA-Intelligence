# Plan: QA-Intelligence → Toàn Năng Test Agent

Date: 2026-08-05 (v2 — cập nhật sau khi ADR-018/SPEC-108 được thêm vào spec baseline)
Base: audit của `codex/requirement-review-agent` working tree + spec đã sửa trong phiên này.
Goal giữ nguyên: agent test toàn năng — mọi loại task, mọi business, kết nối được Claude/Cursor/Codex qua MCP.

## 0. Công nghệ đang dùng (bằng chứng: package.json, ADR-011)

- TypeScript strict + Node.js 24 LTS, ESM, không framework.
- Dependency thật duy nhất: `ajv` + `ajv-formats` (JSON Schema 2020-12). Không LLM SDK nào cài — Reasoning Provider hiện chỉ có bản scripted/replay, chưa gọi model thật.
- Persistence: SQLite (`node:sqlite`, **experimental**) local-first mặc định; Postgres optional cho shared/team profile (ADR-017).
- Test: Node built-in test runner (`node --test`), không Jest/Vitest.
- CI (`.github/workflows/repository-validation.yml`): validate governance (Python) → validate schema → typecheck → test → `npm audit`. Không gate GOV-012.

### Dùng làm plugin Claude Code / Codex / Cursor được chưa?

**Chưa.** Kiến trúc đã thiết kế đầy đủ (ADR-016) nhưng **0% code MCP tồn tại** — `plugins/README.md` ghi "Not started", grep repo không có dòng MCP nào.

Thiết kế khi làm xong (ADR-016 §2):
```
Host (Claude Code/Codex/Cursor) → Host Integration Package (thin) → MCP Interface → Agent Runtime/Evaluation Engine → Skills/Rules/Knowledge
```
- Host Integration Package **không chứa business logic** — chỉ metadata, MCP config, vài Skill/Rule mỏng. Logic thật dùng chung 1 core cho cả 3 host.
- Claude Code: `.claude-plugin/plugin.json` + hooks + MCP config.
- Codex: `.codex-plugin/plugin.json` + MCP config.
- Cursor: MCP config (project/global) + Cursor Rules.
- Transport: local `stdio`/socket (cá nhân) hoặc remote Streamable HTTP + OAuth (team).
- ADR-016 §8 tự khoá thứ tự: chỉ code MCP sau khi 1 capability lõi xong vertical; chỉ bật production sau GOV-012 G1–G4; public release cần thêm G5–G6.

→ Đây là **Giai đoạn 2** dưới đây, chưa bắt đầu.

## 1. Tình trạng thật (bằng chứng, không đoán)

### Đã implement (code thật, có test, 190/190 pass)
- Requirement Review Skill (`src/requirement-review/`): auth → discovery → deterministic rules → bounded LLM reasoning. **1/13** product capability (SPEC-203 trong SPEC-201..213).
- Agent Runtime lifecycle in-memory (`src/runtime/in-memory-agent-runtime.ts`, SPEC-606/508): state machine đầy đủ, idempotency, revision, budget, cancel.
- Evaluation Campaign: runner (1 trial) + coordinator (multi-trial, bounded parallel) + repository (in-memory) + record-store (SQLite thật qua `node:sqlite`, Postgres qua fake transaction manager).
- ADR-017: **SQLite-per-Workspace là default**, Postgres optional cho shared/team profile.
- Deterministic/replay adapters: `ScriptedEvaluationAdapter`, `ScriptedReasoningProvider`, `DeterministicWorkspaceAuthorizer`, `InMemoryKnowledgeSearch` — dùng cho test, không phải production.

### Đã sửa trong phiên này (spec-only, chưa có code) — ADR-018
- **SPEC-108 Memory Model** (mới): Memory tách bạch khỏi Knowledge Store — 2 tầng (Working/run-scoped, Session/TTL-scoped), đều non-authoritative.
  - §7.1 Save Decision Criteria: agent tự đánh giá reuse-likelihood/novelty/cost-of-being-wrong/provenance trước khi quyết lưu — mặc định KHÔNG lưu.
  - §7.3 Failure-Avoidance Retention: lỗi/defect/verdict sai tự sinh candidate tránh lặp lại (đáp ứng yêu cầu "tự học sau mỗi lần test").
  - §4.3 Applicability Scope: project-scoped (mặc định) vs cross-project/global (cần nhiều Workspace corroborate qua SPEC-105, không tự động).
- **AP-063 Proportional Rigor** + **AP-064 Context/Cost Efficiency** (ARCHITECTURE_PRINCIPLES.md): rigor tỷ lệ theo consequence class; bắt buộc reuse context/retrieval trong 1 run.
- **SPEC-508** thêm bảng budget cụ thể (token/time/tool-call) theo consequence class — trước đây chỉ có chữ "budget SHALL exist", không có số.
- **SSOT fix**: SPEC-107 §5 là canonical owner cho AI-adversarial-testing coverage (SPEC-206/213 reference); SPEC-210 §4 là canonical owner cho execution outcome vocab, thêm `flaky` là outcome riêng biệt (SPEC-209 reference).
- **SPEC-105** thêm §9a: phân biệt lỗi 1 lần (path nhanh qua SPEC-108) vs lỗi lặp lại (vẫn qua full governed candidate lifecycle).
- Toàn bộ index/matrix liên quan đã update, `python3 tests/validate_repository.py` + `node tests/validate-json-schemas.mjs` pass.

### Spec có, code KHÔNG có (Spec Only) — không đổi so với trước, cộng thêm SPEC-108
- 12/13 product capabilities: Discovery(201), Requirement Intelligence(202), Business Analysis(204), Risk Analysis(205), Test Strategy(206), Test Design(207), Test Data(208), Automation(209), Execution(210), Bug Analysis(211), Reporting(212), Agent/Skill Quality(213).
- **SPEC-108 Memory Model** — mới viết, 0% code. Đây là spec nền cho yêu cầu "agent tự nhớ, tự học từ lỗi cũ" — chưa có Working Memory, Session Memory, save-decision logic, hay failure-avoidance loop nào implement.
- Interfaces: Rule Engine(502), Plugin(503), Execution Engine(504), Event(505), Skill(509), Tool(510) — chỉ type mirror rải rác, không module/conformance suite riêng.
- Knowledge Repository (SPEC-401), Ontology Repository (SPEC-408) — không persistence/query component.
- Judge calibration/disagreement/drift (SPEC-310 §1/§6) — chưa có dòng code nào.
- MCP host-neutral facade + Claude Code/Cursor/Codex packages (ADR-016) — 0% code (xem §0 trên).
- `agents/`, `skills/` (thư mục promotion) — trống, chưa "tốt nghiệp" GOV-012.
- GOV-012 G1–G6 evidence — không tồn tại, không có trong CI.

### Rủi ro kỹ thuật đang mở
- `node:sqlite` experimental — cần quyết định trước khi vào production (xem §5).
- Postgres adapter chưa test với DB thật, chưa có `pg` driver trong package.json.
- SPEC-108 hiện chỉ là spec — nếu không implement kịp trước khi Giai đoạn 3 mở rộng ra 12 capability, memory/cost sẽ bị "để dành sửa sau" giống lỗi ban đầu.

## 2. Nguyên tắc giữ khi thay đổi

- Không đặt business rule vào prompt, không đặt provider behavior vào core module.
- Domain modules chỉ nói chuyện qua interface (Repository/Runtime/Adapter); SQLite/Postgres, Working/Session Memory đều là adapter/tầng ngang hàng sau seam, không rò rỉ chi tiết lên trên (ADR-017 §6, SPEC-108 §10).
- Rigor tỷ lệ theo consequence class (AP-063) — không áp dụng đồng loạt mọi thao tác như trước.
- Reuse context/retrieval trong 1 run là bắt buộc, không phải optimization tùy chọn (AP-064).
- Mỗi capability mới: spec accepted → deterministic core → adapter thật → GOV-012 evidence → "tốt nghiệp" lên `agents/`/`skills/`/`core/`/`api/`.
- Memory: mặc định KHÔNG lưu; chỉ lưu khi qua được §7.1 criteria; project-scoped là default, cross-project cần governed corroboration — không suy diễn ngược.
- Tracer bullet (Requirement Review) là khuôn mẫu cho 11 capability còn lại — lặp lại pattern, không phát minh lại.

## 3. Checklist theo giai đoạn

### Giai đoạn -1 — Spec quality (HOÀN THÀNH trong phiên này)
- [x] ADR-018 + SPEC-108 (Memory Model) + AP-063/AP-064 + SSOT fix (flaky, AI-testing coverage) + SPEC-105 §9a.
- [x] Toàn bộ index/matrix cập nhật, validator pass.

### Giai đoạn 0 — Vá tài liệu còn lại
- [x] ROADMAP.md: đã phản ánh ADR-017 (SQLite default) và ADR-018 (spec-quality update) — xong trong phiên này.
- [x] 0.1 Quyết định `node:sqlite` experimental: **giữ `node:sqlite`, không đổi `better-sqlite3`** — lý do: API tương đương, native binding vi phạm ADR-011 §5 (portability review) và phá vỡ baseline 2-dependency; rủi ro kiểm soát bằng `engines` pin (`>=24 <25`) + record-store đã có provider-neutral seam. Ghi vào ADR-017 §8 (v1.1.0).

### Giai đoạn 1 — Đóng nốt tracer bullet hiện tại (Requirement Review) trước khi nhân rộng
- [x] 1.1 SPEC-511 case còn thiếu: **code (`ScriptedEvaluationAdapter`, kể cả `replay()`) đã generic và hỗ trợ đủ mọi op từ trước — gap thật chỉ là thiếu test.** Đã thêm 5 test vào `tests/evaluation/scripted-evaluation-adapter.test.ts`: cancellation (failure code `cancelled`, không retry), replay không ghi đè trial gốc, replay báo `divergences`/`achieved_fidelity` tường minh thay vì im lặng khớp, `replay_unavailable` khi thiếu dependency, trial-isolation (1 trial không thể nhận/lộ scripted case của trial khác). 195/195 test pass, validator sạch.
- [x] 1.2 Postgres: đã cài PostgreSQL 18 local (brew `postgresql@18`, port 5433, song song không đụng `postgresql@14` sẵn có trên port 5432). Viết `PgTransactionManager` thật (`src/evaluation/pg-transaction-manager.ts`, dùng `pg` driver — thêm dependency, pure-JS không native binding nên không vi phạm ADR-011 §5). Migration `0001_evaluation_campaign_store.up.sql` áp lên DB thật. Test mới `tests/evaluation/pg-transaction-manager.real.test.ts` (skip sạch nếu thiếu `QA_INTELLIGENCE_TEST_POSTGRES_URL`, không chặn CI không có DB): chạy full shared contract suite + 3 test riêng — concurrent-writer race (`stale_revision` đúng 1 lần), RLS chặn query thiếu Workspace scope (phải dùng role non-superuser `qa_intelligence_app` vì Postgres superuser luôn bypass RLS/FORCE RLS), state sống sót qua `PgTransactionManager` mới (giả lập restart). 12/12 pass với DB thật.
- [x] **1.2b (fix nghiêm trọng, review 2026-08-05) CI job chạy suite Postgres thật, không còn skip 100%**: gap review phát hiện — `QA_INTELLIGENCE_TEST_POSTGRES_URL` không được set ở đâu trong `.github/workflows/repository-validation.yml`, nên RLS/concurrent-writer/restart test (đã viết ở 1.2) chưa từng chạy tự động, chỉ khi dev tự set biến local. Thêm job `postgres-adapter` riêng vào workflow: dựng `postgres:18` service container, áp migration `0001_evaluation_campaign_store.up.sql`, tạo role non-superuser `qa_intelligence_app` (CI không có sẵn role này — GRANT đúng 4 bảng, không GRANT thừa) để RLS test có ý nghĩa thật (superuser luôn bypass RLS bất kể FORCE), rồi chạy `pg-transaction-manager.real.test.ts` với connection string trỏ role đó. Không sửa job `validate` gốc (vẫn database-free, nhanh) — tách job song song riêng cho phần cần DB thật.
- [x] 1.3 Agent Runner durable persistence: **seam mới `AgentRunRecordStore` (`src/runtime/agent-run-record-store.ts`) + `SqliteAgentRunRecordStore` (`src/runtime/sqlite-agent-run-record-store.ts`)** theo đúng pattern Evaluation Campaign, cộng shared contract suite (`tests/runtime/agent-run-record-store-contract.ts`) + test SQLite cụ thể, 8/8 pass. Postgres adapter cho Agent Run (song song SQLite) chưa làm — chỉ SQLite theo đúng local-first default (ADR-017).
- [x] **1.3b (fix nghiêm trọng, review 2026-08-05) Compose `InMemoryAgentRuntime` qua `AgentRunRecordStore`**: gap 1.3 đã đóng. Thay vì copy nguyên pattern `PersistedEvaluationCampaignRepository` (viết lại toàn bộ state machine, rủi ro lệch hành vi cao vì `in-memory-agent-runtime.ts` phức tạp hơn nhiều), chọn cách rủi ro thấp hơn: thêm 1 hook `RunPersistedHook` fire đúng 1 lần sau mỗi command public hoàn tất (`start`/`execute`/`approve`/`resume`/`cancel`, không phải mỗi event trung gian — khớp đúng bản chất atomic-per-command của `retainMutation`), cộng method `seed()` để nạp lại state từ store. `src/runtime/persisted-agent-runtime.ts` (`PersistedAgentRuntime`) compose `InMemoryAgentRuntime` + hook đó, ghi qua `AgentRunRecordStore` thật, có `restore()` để khôi phục sau restart. State machine logic **không bị nhân đôi** — chỉ 1 nguồn duy nhất trong `InMemoryAgentRuntime`, đã verify 48 test cũ vẫn pass sau khi thêm hook (hành vi mặc định không đổi khi không truyền hook). Trong lúc viết test tích hợp thật, phát hiện 2 giả định sai trong `sqlite-agent-run-record-store.ts` (chưa từng lộ ra vì contract test cũ dùng fixture giả, không dùng `InMemoryAgentRuntime` thật): (a) `validateMutation` ép "start luôn revision=1", nhưng runtime thật đạt revision=3 ngay sau start (5 event nội bộ); (b) `decodeRecord` ép "số event = revision", cũng sai vì đây là 2 khái niệm độc lập (revision = transition counter, không phải event count). Sửa cả 2 chỗ, giữ nguyên ràng buộc thật cần (event sequence liên tục, revision tăng đúng 1 mỗi mutation kế tiếp). 3 test tích hợp mới (`tests/runtime/persisted-agent-runtime.integration.test.ts`) dùng `SqliteAgentRunRecordStore` thật (không mock): start → tạo instance runtime mới (giả lập restart) → `restore()` → inspect thành công; cancel sau restart → instance thứ 3 đọc lại thấy state `cancelled`; restore run chưa từng tồn tại → `not_found`. 275 test total (274 pass + 1 skip), validator sạch, 0 vulnerability.
- [x] **1.4 Implement SPEC-108 Memory cho Requirement Review**: `src/memory/working-memory.ts` (`WorkingMemoryKnowledgeSearch` — decorator quanh `KnowledgeSearch`, cache theo digest của query/scope/applicability/snapshot, invalidate tự động khi bất kỳ durable reference nào đổi, đúng AP-064 + SPEC-309 §4) và `src/memory/session-memory.ts` (`SessionMemory` — áp đúng 4 tiêu chí §7.1 trước khi lưu, chỉ cho fast path khi low-consequence + project-scoped theo §7.2/§4.3, TTL fail-safe theo §9, `reject()`/`invalidateWorkspace()` theo §7.2/§8). 14 unit test (`tests/memory/`) + 2 integration test chứng minh thật với `AssessRequirementQuality` (2 lần review cùng nội dung → Knowledge Search chỉ chạy 1 lần; review khác nội dung → chạy lại đúng 2 lần). Không sửa `in-memory-agent-runtime.ts` — Working Memory áp ở lớp `KnowledgeSearch` (đúng interface seam, không cần đụng runtime 1724 dòng). 220 test total (219 pass + 1 skip), validator sạch.
- [~] 1.5 GOV-012 G1–G4 evidence cho Requirement Review Skill: **cập nhật `governance/reviews/requirement-review-tracer-bullet/IMPLEMENTATION_EVIDENCE.yaml` + `GOV-012_GATE_RECORD.yaml`** phản ánh đúng công việc 1.1–1.4 (Postgres driver thật, Agent Run record-store, Memory) — thêm increment review mới, đóng các "pending" đã thật sự xong, số liệu validation cập nhật (219 pass/1 skip, 0 vulnerability). **Không thể full pass** — G2 (production OIDC), G3 (production Evaluation Adapter, không phải scripted), G4 (dataset đầy đủ, holdout, load test) đều cần công việc lớn hơn nhiều (LLM provider thật, OIDC identity service) ngoài phạm vi hợp lý của 1 phiên; "tốt nghiệp" lên `agents/`/`skills/` **chưa** xảy ra vì các gate chưa pass hoàn toàn — đúng bản chất, giả tạo evidence sẽ vi phạm chính GOV-012 §5 (override phải có approver độc lập, không thể tự cấp).
- [x] 1.6 CI: thêm block mới vào `tests/validate_repository.py` (chạy qua `npm run validate:repository`, đã có sẵn trong CI workflow `.github/workflows/repository-validation.yml` — không cần sửa YAML workflow) — mọi file thật (không phải README) trong `agents/`/`skills/` phải khớp `stem` với 1 subject có trong `GOV-012_GATE_RECORD.yaml` nào đó dưới `governance/reviews/`, nếu không sẽ fail CI. Test thật bằng cách tạo file giả `agents/fake-test-agent.json` — xác nhận fail đúng thông điệp, rồi xoá. Hiện `agents/`/`skills/` chỉ có README nên check pass sạch (chưa cái nào "tốt nghiệp"), nhưng từ giờ ngăn được việc thêm package thật vào đó mà thiếu evidence.
- [x] 1.7 Áp dụng bảng budget SPEC-508 §3.1 vào runtime thật: phát hiện bảng cũ dùng "Low/Medium/High" mơ hồ trong khi `ConsequenceClass` type thật có 4 giá trị (`advisory`/`reversible`/`controlled_side_effect`/`high_consequence`) — sửa spec dùng đúng 4 giá trị trước khi code (`reversible` và `controlled_side_effect` dùng chung mid-tier). Viết `src/runtime/default-budgets.ts` (`defaultAgentRunBudgets()`, `resolveAgentRunBudgets()` — override thắng field-by-field, không override thì fallback default). Chứng minh thật: build `AgentRunStartRequest` bằng `resolveAgentRunBudgets()` rồi gọi thẳng `InMemoryAgentRuntime.start()` — runtime chấp nhận, `high_consequence` vẫn đúng yêu cầu approval độc lập (budget table không bỏ qua approval gate). 10 test mới (6 unit + 4 integration với runtime thật). 230 test total (229 pass + 1 skip), validator sạch.
- [x] **1.8 (review lần 2, 2026-08-05) Đóng gap thật phát sinh từ chính 1.2b/1.3b vừa fix — không phải spec-only, mà là bug kỹ thuật trong code vừa viết**: dùng 3 agent song song review lại toàn bộ 8 spec/ADR có code + riêng 4 fix vừa làm. Tìm 3 vấn đề thật:
  - **`pendingPersist` "poison pill"** (`src/runtime/persisted-agent-runtime.ts`): thiết kế ban đầu dùng 1 Promise chain DUY NHẤT cho toàn instance — `.then()` không có `onRejected` khiến 1 lần `retainMutation` fail làm MỌI lệnh sau (mọi run khác) vĩnh viễn nhận lại lỗi cũ, dù DB đã hồi phục và in-memory state vẫn tiếp tục đúng. Sửa: chain theo per-run (`#pendingByRun` Map), tự reset về resolved sau mỗi lần settle (thành công hay lỗi) để không lan sang lệnh kế tiếp của CHÍNH run đó.
  - **Race `execute`/`cancel` đọc nhầm record**: khi `execute()` đang `await` executor (chậm), `cancel()` có thể chạy xen vào và thắng trước — đây là hành vi HỢP LỆ (client gọi đồng thời qua network), không phải bug logic. Vấn đề thật chỉ là hệ quả: `retainMutation` sẽ trả `stale_revision`/`invalid_request` cho lệnh thua cuộc, và (do bug pending-persist ở trên) từng bị coi là lỗi thật rồi throw. Sửa `#retain()`: chỉ gọi `retainMutation` khi `record.snapshot.revision === expected_revision + 1` (đúng đủ điều kiện là chính command này gây ra); nếu không khớp (record chưa đổi, hoặc đã bị người khác vượt qua), coi là no-op an toàn, không gọi store. Cộng: `stale_revision`/`idempotency_conflict` từ store cũng được coi an toàn (không throw) vì nghĩa là 1 command khác đã persist state mới hơn rồi.
  - **SPEC-203 §7 verdict mapping sai ngữ nghĩa**: fix trước (1.2b cùng đợt) ép MỌI critical finding thành `"rejected"` bất kể category, nhưng §7 định nghĩa "rejected" hẹp là "conflict or unsafe intent" — category như `testability`/`completeness` severity=critical không mang ý nghĩa đó. Sửa: chỉ `security_and_privacy`/`workspace_safety` map "rejected"; critical category khác map "blocked" (vẫn chặn pass 100% như yêu cầu §9, chỉ đúng nhãn hơn).
  - 3 test tích hợp mới cho `PersistedAgentRuntime` (poison-pill không lan sang run khác/lệnh sau; race thật giữa execute/cancel dùng `DeferredExecutor` không throw sai) + 2 test mới cho SPEC-203 (critical category khác an toàn → blocked; critical trộn lẫn safety+non-safety → safety thắng, vẫn rejected). 280 test total (279 pass + 1 skip), validator sạch, 0 vulnerability.
  - **Chưa fix** (ghi nhận, không giả vờ đủ): whitespace-only `rationale` bypass rule completeness trong `requirement-intelligence-rule-engine.ts` (thiếu `.trim()`); Working Memory cache-key (`working-memory.ts`) không bao gồm đủ trường dùng trong digest (`applicability`/`limit`/`knowledge_snapshot`), gây cache-thrashing khi 2 request xen kẽ khác nhau ở các trường đó nhưng cùng key — cả 2 đều là gap nhỏ, không phải blocker an toàn, để dành tăng sau.

### Giai đoạn 2 — MCP host integration (bắt buộc để "kết nối Claude, Cursor, Codex")
- [x] 2.1 **Bỏ qua SPEC-503 (Platform Plugin) — không phải blocker thật cho MCP.** Đọc kỹ ADR-016 §3 xác nhận: SPEC-503 (Playwright/GitHub/Jira adapter) và "MCP Interface" là 2 khái niệm khác biệt hoàn toàn. Không cần SPEC-503 để làm MCP.
- [x] 2.2 Build MCP facade thật: **quyết định không dùng `@modelcontextprotocol/sdk`** (16 dependency trực tiếp — express/hono/zod/oauth/sse cho surface không cần ở dev-phase) — ghi **ADR-019** tự implement JSON-RPC 2.0 + MCP protocol tối thiểu (`initialize`/`tools/list`/`tools/call`/cancellation), byte-compatible với MCP chuẩn. Code: `src/mcp/jsonrpc.ts`, `src/mcp/protocol.ts`, `src/mcp/mcp-server.ts`, `src/mcp/stdio-transport.ts`, `src/mcp/agent-runtime-tool-registry.ts` (seam dịch `tools/call` → SPEC-508 Agent Runtime, module duy nhất trong `src/mcp/` biết tới Agent Runtime). 22 test mới (jsonrpc, mcp-server theo đúng ADR-019 §8 checklist, tool-registry integration với `InMemoryAgentRuntime` thật).
- [x] 2.3 Host packages thật tại `hosts/` (thư mục mới, tách biệt `plugins/` vì khác khái niệm theo ADR-016 §3): `hosts/claude-code/.claude-plugin/plugin.json`, `hosts/codex/.codex-plugin/plugin.json`, `hosts/cursor/mcp.json.example` — đều trỏ `dist/src/mcp/dev-entrypoint.js`. **Xác nhận thật bằng cách chạy đúng lệnh từ đúng working directory mỗi plugin dùng** — cả 2 chạy thành công qua stdio thật.
- [x] 2.4 (rút gọn) Conformance qua `dev-entrypoint.ts`: 1 request `tools/call` thật (`initialize` → `tools/call assess_requirement_quality`) chạy hết pipeline thật (authorization → Discovery → deterministic rules → evidence) qua stdio, trả `outcome: "completed", verdict: "pass"`. Phát hiện + sửa 1 bug thật trong lúc test: `resolveAgentRunBudgets()` mặc định set `max_tokens`, nhưng `RequirementReviewRuntimeExecutor` không track token usage khi chỉ dùng deterministic rule (không gọi reasoning) → mọi run tự động fail `budget_exhausted`. Sửa: thêm `budgets` override tường minh cho `AgentRuntimeToolDefinition` khi Skill không cần token budget. **Chỉ test 1 host thật (dev script mô phỏng); chưa test qua Claude Code/Codex/Cursor thật cài đặt** (cần môi trường ngoài phiên này).
- [ ] 2.5 stdio đã xong (dev); remote transport (OAuth) theo ADR-016 §8 — chưa làm, thuộc shared/team profile, chưa cần cho local-first default.
- [ ] 2.6 Memory Workspace-scope qua transport thật — chưa làm; `SessionMemory` (1.4) đã test Workspace-scope ở unit-level nhưng chưa nối vào MCP tool call nào.

### Giai đoạn 3 — Nhân rộng ra toàn bộ QA lifecycle (12 capability còn lại)
Lặp lại pattern tracer-bullet cho từng capability, **mỗi capability giờ bắt buộc bao gồm Memory (SPEC-108) và Proportional Rigor (AP-063) ngay từ đầu**, không để dành sửa sau như Requirement Review đã bị:

- [x] 3.1 **Discovery (SPEC-201) — tracer-bullet scope: Knowledge Store search only.** `src/discovery/public.ts` + `src/discovery/discover-product-context.ts` (`DiscoverProductContext` deep module): auth → search Knowledge Store theo từng scope độc lập → `DiscoveryReport` với Known/Unknown Register, Clarification Questions (chỉ hỏi khi scope thật sự rỗng), phân biệt rõ fact/inference/assumption/question (§66). Unavailable source → `limitations` (không phải "tính năng không tồn tại", đúng §10). Conflict Register có trong contract nhưng luôn rỗng — chưa có logic phát hiện mâu thuẫn semantic thật, không giả tạo. **Chưa làm** (đúng nêu rõ, không giả vờ đủ): SPEC-201 §8 Semantic UI Discovery (Page/Region/Feature/Action semantic model) — cần Platform Plugin (SPEC-503) + browser adapter (Playwright) chưa tồn tại; Product Surface Map, Semantic UI Map (2 trong 8 output của §5) không sinh ra được ở slice này. 7 test mới, 252 pass + 1 skip, validator sạch. Chưa nối Session Memory (1.4) vào Discovery finding — để dành khi có nhu cầu thật từ 1 capability khác cần reuse.
- [~] 3.2 **Requirement Intelligence (202) — tracer-bullet scope: Requirement Contract completeness (§4) + traceability-count check (§11), không phân loại upstream/downstream.** `src/requirement-intelligence/requirement-intelligence-rule-engine.ts` (`RequirementIntelligenceRuleEngine implements DeterministicRuleEngine`) cắm thẳng vào `AssessRequirementQuality` (Skill dùng chung, chỉ đổi rule engine) — không viết lại authorize/discover/reasoning pipeline. 3 rule: (1) thiếu `rationale` → `completeness` finding — field mới thêm vào `Requirement` type + `schemas/requirement.schema.json` (optional, không bắt buộc trong schema để rule engine tự báo thay vì schema reject cứng); (2) status ≥ `in_review` mà 0 traceability edge → `traceability` finding; (3) status ≥ `implemented` mà <2 edge → `traceability` finding cảnh báo cần cả upstream lẫn downstream. **Quyết định rõ ràng không làm**: SPEC-202 §11 nói trace "upstream to intent" và "downstream to risk/design/rule/test/...", nhưng `traceability[].relationship` là free string không có enum accepted — rule engine không bịa taxonomy, chỉ đếm số edge theo ngưỡng lifecycle status. §9 duplicate/conflict detection cũng không làm — Skill hiện chỉ đánh giá 1 Requirement/lần gọi, không có input đa-requirement để so sánh. Bỏ qua SPEC-204 Business Analysis lần này (workflow/actor model cần thiết kế contract riêng, quá rộng cho 1 increment). 9 test mới (`tests/requirement-intelligence/`), 270 test total (269 pass + 1 skip), validator sạch, 0 vulnerability. Business Analysis (204) và traceability-direction taxonomy để dành cho tăng sau khi có nhu cầu thật.
- [~] 3.3 **Risk Analysis (205) — tracer-bullet scope: Risk Model completeness (§2) + critical-category-not-hidden rule (§5/§9), theo đúng pattern SPEC-203/202.** Khác 202 (tái dùng `Requirement` có sẵn), SPEC-205 không có type/schema nào tồn tại trước — tạo mới hoàn toàn: `schemas/risk.schema.json` + `schemas/risk-assessment.schema.json`, `src/risk-analysis/public.ts` (types: `Risk`, `RiskAssessment`, `RiskFinding`...), `src/risk-analysis/assess-risk-quality.ts` (`AssessRiskQuality` — skill riêng theo cùng pipeline authorize→discover→rule→evidence như `AssessRequirementQuality`, nhưng KHÔNG có nhánh bounded-reasoning vì SPEC-205 §8 AI Assistance chỉ "MAY suggest risks", nằm ngoài scope; `RiskQualityRuleEngine` — 5 rule: statement đủ cause/event/consequence, có owner, có controls (thiếu → **critical**, category `treatment_governance`), có residual_risk statement (thiếu → **critical**), có likelihood/impact rationale (thiếu → medium). Verdict mapping học đúng bài học vừa fix ở SPEC-203: critical finding luôn chặn pass, nhưng chỉ category `treatment_governance` (đại diện "no control"/"no residual statement" — tương ứng ý "cannot accept this risk's exposure" của §5) map "rejected"; category khác map "blocked" nếu sau này có category critical khác xuất hiện. **Quyết định rõ ràng không làm**: §5 scoring/prioritization (chưa có thang đo governed nào được chấp nhận để tính điểm); §7 traceability-to-requirements/tests/defects (Skill chỉ xét 1 Risk/lần gọi, không có corpus để trace); §8 AI-assisted risk discovery (cần reasoning provider thật gợi ý risk mới, chưa có); §6 Treatments lifecycle (avoid/reduce/transfer/accept/monitor) — field `treatment` có trong type/schema (optional) nhưng chưa có rule nào validate quy trình chấp thuận accept-risk. 19 test mới (`tests/risk-analysis/`), 292 test total (291 pass + 1 skip), validator sạch (28 schema compile), 0 vulnerability.
- [~] 3.4 **Test Strategy (206) + Test Design (207) + Test Data (208) — tracer-bullet scope: Contract completeness cho cả 3, cùng pattern verdict category-aware như 203/205.** Làm đủ 3 spec 1 lần (khác 202/205 chỉ 1 spec) vì cả 3 phụ thuộc chuỗi thật và có Contract §y hệt cấu trúc.
  - **207 Test Design** (làm trước, dùng `schemas/test-case.schema.json` có sẵn — chỉ có schema thật trong 3 spec): thêm field thiếu vào schema (`actor_scope`/`priority`/`tags`/`cleanup`, optional). `src/test-design/` (`AssessTestCaseQuality` + `TestCaseQualityRuleEngine`) — completeness §2 + rule "`expected_results[].authority` không rỗng" (§3 "expected results SHALL derive from authority") → category `authority`, critical, map "rejected" (khác category khác map "blocked"). Không làm: technique selection (§4), coverage normal/alternate/boundary/failure (§3, thuộc tính 1 bộ test case chứ không phải 1 case), "observable" assertion thật (cần LLM).
  - **206 Test Strategy**: hoàn toàn mới — `schemas/test-strategy.schema.json` (12 mục §3 Strategy Contract). `src/test-strategy/` (`AssessTestStrategyQuality` + `TestStrategyQualityRuleEngine`) — completeness 12 mục + rule "nếu có `governing_risk_refs` mà không có `residual_risk`" → category `risk_coverage`, critical, "rejected" (diễn giải hẹp lại §6 "critical risks SHALL have explicit prevention/detection evidence" thành điều rule engine thật kiểm định được — không tự phán risk nào "critical" từ ref string). Không làm: technique/test-level phù hợp thật với risk profile (§6, cần đánh giá), coverage completeness qua corpus (§7).
  - **208 Test Data**: hoàn toàn mới — `schemas/test-dataset.schema.json` (§4 Data Contract). `src/test-data/` (`AssessTestDatasetQuality` + `TestDatasetQualityRuleEngine`) — completeness §4 + rule an toàn cụ thể nhất trong 3: `contains_sensitive_fields=true` mà không có `sensitive_field_controls` → category `privacy_and_isolation`, critical, "rejected" (đúng §7 "sensitive fields SHALL be minimized, masked, access-controlled, encrypted"). Không làm: reproducibility thật (§5, cần execution evidence), cross-Workspace isolation test thật (§6, cần chạy thử truy cập trái phép chứ không phải review tài liệu), masked-data transform correctness (§3, cần dữ liệu thật, ngoài phạm vi 1 Skill advisory chỉ đọc metadata).
  - Cả 3 skill dùng chung pattern authorize→discover→rule→evidence (không bounded-reasoning, vì cả 3 §8/§9 AI-assist chỉ "MAY suggest", ngoài scope). Verdict mapping category-aware học đúng từ fix SPEC-203 lần review — áp dụng ngay từ đầu, không đợi retrofit như 203 đã bị.
  - 3 schema mới thêm (`test-case-assessment`, `test-strategy` + `test-strategy-assessment`, `test-dataset` + `test-dataset-assessment`) + 1 schema sửa (`test-case.schema.json` thêm field optional). 34 test mới (`tests/test-design/`, `tests/test-strategy/`, `tests/test-data/`). 326 test total (325 pass + 1 skip), 33 schema compile, validator sạch, 0 vulnerability.
  - **Chưa làm cho cả 3** (nêu rõ, không giả vờ đủ): traceability-completeness qua corpus thật (mỗi Skill chỉ xét 1 artifact/lần gọi); AI-assisted content generation (§6/§8/§9 tùy spec); phần biến "review agent" thành "test agent" **thật sự chạy test** vẫn thuộc 3.5 (Automation/Execution, SPEC-209/210) — 3.4 chỉ dừng ở tạo/kiểm chất lượng tài liệu test, chưa thực thi gì.
- [~] 3.5 **Automation (209) + Execution (210) — tracer-bullet scope: quyết định giữ nguyên pattern advisory-document-review (không viết state machine/execution engine thật).** Trước khi code: đọc kỹ SPEC-210 §1 xác nhận nó tự tuyên bố "This Product specification owns user-visible execution intent and canonical result meaning" — KHÔNG sở hữu engine protocol (SPEC-504) hay authoritative runtime transitions (SPEC-602); state diagram §3 chỉ là "explanatory projection". Vì vậy quyết định KHÔNG viết lifecycle component thật kiểu `InMemoryAgentRuntime` (sẽ đúng hơn cho SPEC-602, không phải 210) — giữ đúng pattern 6 capability trước: 1 skill review 1 artifact document/lần gọi, không thực thi gì, không cần Tool Contract (SPEC-510) như lo ngại ban đầu.
  - **209 Automation**: `schemas/automation-asset.schema.json` mới (§3 Automation Asset: implemented test case refs, execution interface, compatible engine, environment constraints, owner, assertion_map, retry_policy). `src/automation/` (`AssessAutomationAssetQuality` + `AutomationAssetQualityRuleEngine`) — completeness + 2 rule an toàn: asset implement test case mà không có `assertion_map` → high `assertion_integrity` (§4 "assertions SHALL map to semantic expected results"); secret literal (`password:`/`api_key:`...) nhúng trực tiếp trong field khai báo thay vì inject qua secret management → **critical** `isolation`, map "rejected" (§8). Không làm: automation có thật sự implement đúng test intent không (§4/§9, cần chạy asset thật), engine/plugin compatibility runtime (§5), flakiness classification (§7, cần nhiều lần chạy thật để so sánh).
  - **210 Execution**: `schemas/execution-record.schema.json` mới (§2 Execution Contract: execution ID/workspace/actor, test+automation version refs, engine/plugin refs, environment/dataset refs, lifecycle `state`, `outcome` nullable theo §3/§4). `src/execution/` (`AssessExecutionRecordQuality` + `ExecutionRecordQualityRuleEngine`) — completeness + rule honesty quan trọng nhất trong tăng này: state terminal mà không có outcome, hoặc outcome đã gán khi state chưa terminal → high `outcome_integrity`; **outcome="passed" mà evidence rỗng → critical, map "rejected"** (§4/§10 "Infrastructure errors and flaky retries SHALL NOT be reported as product passes" — diễn giải cụ thể thành "pass không bằng chứng là verdict không đáng tin"). Không làm: 1 outcome "passed" có thật sự đúng không (cần đọc nội dung evidence, ngoài phạm vi advisory review), scheduling/concurrency (§5), so sánh nhiều lần thử để phân loại flaky/retry (§7, cần dữ liệu nhiều execution).
  - Cả 2 skill review đúng thứ tự phụ thuộc đã đúng spec: `Automation Asset` implement `TestCase` (207) đã có; `ExecutionRecord` trace `automation_asset_ref`+`test_case_ref` — không phát minh khái niệm mới ngoài spec.
  - 4 schema mới (`automation-asset` + assessment, `execution-record` + assessment). 22 test mới (`tests/automation/`, `tests/execution/`). 348 test total (347 pass + 1 skip), 37 schema compile, validator sạch, 0 vulnerability.
  - **Việc thật sự "agent chạy test" vẫn CHƯA làm** — đây là giới hạn có chủ đích: build execution engine/Tool Contract thật là việc Giai đoạn 4 (SPEC-504/510) + SPEC-602 runtime, không nằm trong tracer-bullet advisory pattern đang dùng cho 3.1–3.7. Khi có nhu cầu thật (ví dụ 1 host cụ thể cần agent chạy Playwright thật), quay lại quyết định này.
- [~] 3.6 **Bug Analysis (211) + Reporting (212) — tracer-bullet scope: Contract completeness, cùng pattern advisory 8 capability trước.**
  - **211 Bug Analysis**: `schemas/defect.schema.json` mới (§2 Defect Contract — `suspected_cause`/`confirmed_cause` tách 2 field riêng biệt đúng yêu cầu "kept distinct"). `src/bug-analysis/` (`AssessDefectQuality` + `DefectQualityRuleEngine`) — completeness + 2 rule: `confirmed_cause` không có evidence → high `cause_integrity` (§6 "AI MAY suggest hypotheses but SHALL not present them as confirmed"); status đóng (`fixed`/`verified`/`closed`) mà thiếu `fix_evidence`/`regression_validation_ref`/`release_ref` → **critical** `closure_governance`, map "rejected" (§8 "a defect closes only when fix evidence, regression validation, impacted artifacts, and release identity are recorded" — đóng sai quy trình có thể che giấu vấn đề chưa xử lý xong). Không làm: root cause có đúng thật không (§6, cần domain judgment), reproduction reliability thật (§3), learning candidate generation (§7, thuộc SPEC-105).
  - **212 Reporting**: `schemas/report.schema.json` mới (§4 Report Contract, §5 Metric field đầy đủ owner/definition/numerator/denominator/source/cadence). `src/reporting/` (`AssessReportQuality` + `ReportQualityRuleEngine`) — completeness + rule quan trọng nhất: `critical_exceptions` khai báo nhưng không xuất hiện trong `findings` → **critical** `aggregation_integrity`, map "rejected" (§6 "critical failures... SHALL remain visible regardless of overall score" — diễn giải cụ thể "khai báo critical exception mà báo cáo không nhắc tới = có thể bị điểm tổng hợp che giấu"). Không làm: metric numerator/denominator tính đúng chưa (§5, cần dữ liệu thật), historical reproducibility (§10), narrative summary fabrication check (§7, cần reasoning provider thật để so sánh).
  - 4 schema mới (`defect` + assessment, `report` + assessment). 22 test mới (`tests/bug-analysis/`, `tests/reporting/`). 370 test total (369 pass + 1 skip), 41 schema compile, validator sạch, 0 vulnerability.
  - **Vòng lặp học từ lỗi (SPEC-108 §7.3 Failure-Avoidance Retention, SPEC-105 §9a recurrence) chưa nối thật** — increment này chỉ tạo ra `Defect`/`Report` document có thể review chất lượng, chưa có consumer nào tự động biến 1 defect verdict sai thành avoidance-fact candidate. Để dành khi có nhu cầu thật kết nối 2 module đã có sẵn (`src/memory/failure-avoidance.ts` từ 1.4/1.8 và `src/bug-analysis/` mới).
- [~] 3.7 **Agent/Skill Quality Assessment (213) — KHÁC HẲN 3.1–3.6: không phải document-review advisory, là meta-capability dùng chính hạ tầng Evaluation Campaign (SPEC-107/511/606/607) đã có từ Giai đoạn 1.** Đọc kỹ SPEC-213 §6 xác nhận output (`recommend_release`/`recommend_conditional_release`/`reject_release`/`indeterminate`) trùng khớp `EvaluationManager` (SPEC-411, `src/evaluation/evaluation-manager.ts`) đã build sẵn từ trước — không tạo schema/type mới, không viết pattern advisory lần thứ 9.
  - Quyết định phạm vi: chỉ nối `EvaluationManager` (đã có) với `TrialResult[]` sinh ra TỪ VIỆC GỌI THẬT 1 Skill đã build (`AssessTestCaseQuality`, SPEC-207) — không viết full `EvaluationAdapter` 7-operation (SPEC-511, việc lớn hơn nhiều, tương đương 1 adapter mới ~500+ dòng) vì đã có `ScriptedEvaluationAdapter` test riêng cho contract đó rồi; giá trị dogfooding thật nằm ở "subject được đánh giá là có thật, không phải mock".
  - `src/agent-skill-quality/evaluate-test-case-quality-skill.ts` (`evaluateTestCaseQualitySkill()`): gọi `AssessTestCaseQuality.review()` thật cho từng case (positive: test case đủ field; negative: thiếu `expected_results[].authority`), dịch `TestCaseReviewResult` thật thành `TrialResult` (`outcome`/`failure_class` phản ánh đúng "verdict Skill trả về có khớp kỳ vọng của case không" — không đoán trước kết quả), rồi feed vào `EvaluationManager.evaluate()` thật để sinh verdict+recommendation. 1 critical invariant thật (`no-negative-case-wrongly-accepted`): Skill không được chấp nhận case cố tình thiếu field quan trọng.
  - Chứng minh bằng 3 test: (1) Skill thật đánh giá đúng cả 2 case → `recommend_release`; (2) tiêm 1 rule engine "hỏng" (luôn báo satisfied, giả lập regression thật) → trial thất bại đúng, `reject_release`, invariant fail; (3) authorization bị từ chối → trial `blocked`/`policy_denial`, không fabricate pass. 3 test mới, không thêm schema (dùng lại 41 schema có sẵn). 373 test total (372 pass + 1 skip), validator sạch, 0 vulnerability.
  - **Chưa làm** (nêu rõ): full `EvaluationAdapter` production wrap cho từng Skill (§7 acceptance criteria đầy đủ — cần cho production Evaluation Campaign chạy Skill thật qua Runner/Coordinator, không chỉ gọi trực tiếp như increment này); Judge/Oracle hierarchy thật (§9, cần reasoning provider thật); dataset governance holdout/contamination check (SPEC-107 §6); mở rộng dogfooding sang 8 Skill còn lại (chỉ chứng minh với 1 Skill, đủ để xác nhận pattern khả thi, chưa lặp lại cho toàn bộ).
  - **Hoàn tất 12/12 capability Giai đoạn 3 (3.1–3.7).** Bước tiếp theo hợp lý: quay lại Giai đoạn 4 (interface nền 502/504/505/509/510) hoặc GOV-012 evidence thật cho các Skill đã "đủ tốt nghiệp" một phần.

Mỗi mục 3.x: deterministic core → Memory save-decision cho observation loại đó → adapter thật → GOV-012 evidence → promote lên `agents/`/`skills/`.

### Giai đoạn 4 — Interface/Component nền còn thiếu (chặn nhiều capability ở giai đoạn 3)
- [ ] 4.1 Rule Engine Interface (502) thật — tách khỏi `assess-requirement-quality.ts` để tái dùng cho 202/204/205.
- [ ] 4.2 Execution Engine Contract (504) — cần cho 209/210.
- [ ] 4.3 Event Contract (505) — observability xuyên suốt.
- [ ] 4.4 Skill Contract (509) + Tool Contract (510) thật.
- [ ] 4.5 Knowledge Repository (SPEC-401) persistence thật — thiếu 6 command + 5 query (CreateDraft, Revise, SubmitReview, RecordDecision, Deprecate, Archive...).
- [ ] 4.6 Ontology Repository (SPEC-408) — component đọc/validate/serve `ontology/*.yaml`.
- [ ] 4.7 Judge calibration/disagreement/drift (SPEC-310 §1/§6).
- [~] **4.8 (mới) Memory component thật** (SPEC-108) — Working Memory + Session Memory store dùng chung đã có từ 1.4. Increment này thêm 2 phần còn thiếu: **§7.3 Failure-Avoidance Retention** (`src/memory/failure-avoidance.ts` — `evaluateFailureAvoidanceCandidate()`: nhận causal-mistake candidate + cờ `recurring`/`applicability_scope`, chỉ retain qua fast path khi one-off + project-scoped + low-consequence, ngược lại decline `requires_learning_engine` đúng ranh giới SPEC-105 §9a, không tự làm recurrence-detection) và **§11 Observability hợp nhất** (`src/memory/observability.ts` — `reportMemoryObservability()` gộp `WorkingMemoryKnowledgeSearch.reuseStats()` + `SessionMemory.stats()` mới thêm — lifetime promotions/expiries/async_rejections/declines_by_reason — thành 1 report). 9 test mới (`tests/memory/failure-avoidance.test.ts`, `tests/memory/observability.test.ts`), 261 test total (260 pass + 1 skip), validator sạch, 0 vulnerability. **Chưa làm**: §6 corpus-scale ranking/exclusion (chưa cần — chưa có Knowledge Store lớn thật để đo), durable/shared persistence cho Session Memory (hiện vẫn in-process Map như 1.4, chưa theo pattern record-store như Evaluation Campaign/Agent Run), nối failure-avoidance vào 1 Skill thật (chưa có consumer gọi `evaluateFailureAvoidanceCandidate` từ runtime — để dành khi 1 capability giai đoạn 3 cần).

## 4. Thứ tự đề xuất tổng thể

1. Giai đoạn -1 — xong.
2. Giai đoạn 0 — còn 1 quyết định (`node:sqlite`), làm trước khi 1.2/1.3 chạm Postgres/SQLite thật.
3. Giai đoạn 1 (đóng tracer bullet, **gồm cả Memory 1.4**) — bắt buộc trước khi nhân rộng, tránh nợ kỹ thuật lan ra 12 capability sau đúng như đã xảy ra với Postgres-only trước đây.
4. Giai đoạn 4.8 (Memory component thật) nên làm ngay sau 1.4 chứng minh được pattern, trước khi mở Giai đoạn 3 — vì 3.1 Discovery đã cần Memory ngay.
5. Giai đoạn 2 (MCP) — sau khi 1 Skill graduate, trước khi mở rộng Giai đoạn 3, để mỗi capability mới có MCP conformance test từ đầu thay vì retrofit.
6. Giai đoạn 4 (còn lại: 502/504/505/509/510/401/408) xen kẽ Giai đoạn 3 — build đúng lúc có consumer thật.
7. Giai đoạn 3 lặp 3.1 → 3.7, mỗi vòng là 1 tracer-bullet mini kèm Memory + Proportional Rigor từ đầu.

## 5. Việc cần quyết định từ bạn (owner) trước khi code

- [ ] `node:sqlite` experimental — chấp nhận rủi ro hay đổi `better-sqlite3`?
- [ ] Thứ tự: Memory (1.4/4.8) và MCP (giai đoạn 2) cái nào trước? Plan đề xuất Memory trước (vì Giai đoạn 3 cần nó ngay từ Discovery), MCP sau. Nếu mục tiêu gần là demo kết nối Claude/Cursor/Codex sớm, có thể đảo.
- [ ] Trong giai đoạn 3, capability nào ưu tiên nghiệp vụ thật (có use case cụ thể chờ) — điều chỉnh thứ tự 3.1–3.7 theo đó thay vì thứ tự phụ thuộc lý thuyết.
- [ ] SPEC-108 §7.1/§7.3 (save-decision, failure-avoidance) cần threshold cụ thể (bao nhiêu lần lặp mới coi là "recurring" theo SPEC-105 §9a?) — hiện spec chỉ nói "SHALL", chưa có số, giống lỗ hổng budget đã vá ở SPEC-508. Cần quyết định số cụ thể khi implement 1.4.
