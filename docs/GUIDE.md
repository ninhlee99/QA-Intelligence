# QA Intelligence — Install & Usage Guide

Derived documentation. Authoritative sources: `README.md`, `hosts/README.md`,
`api/README.md`, `src/mcp/dev-entrypoint.ts`, `src/mcp/remote-dev-entrypoint.ts`,
ADR-016, ADR-020, GOV-012. Generated by manual review of those sources
(2026-08-07). If this file and a source above disagree, the source wins.

## 0. Status — read this first

QA Intelligence has **no production API and no product UI**. `api/README.md`
says production API implementation has not started. What you *can* run today
is a **development-only MCP server** (`0.1.0-dev`) exposing a Senior QA tool
catalog (discover → generate/execute → a11y naming smoke → draft defects →
release gate). See `hosts/README.md` for the live tool list. Auth is a
fixture verifier (stdio) or self-minted OIDC (remote) — not production IdP.
Knowledge Store is an in-memory seed. **GOV-012 G2–G6 still block production
enablement and public release** (ADR-016 §8). This guide documents the
dev slice that actually runs.

There are two transports:

- **Local `stdio`** (`dev-entrypoint.ts`) — one process per Workspace, used by
  Claude Code / Codex / Cursor as a local MCP server.
- **Remote Streamable HTTP** (`remote-dev-entrypoint.ts`) — same runtime,
  reachable over `http://127.0.0.1:8787/mcp`, with real signed OIDC tokens
  from a self-hosted dev JWKS (still non-production membership).

## 1. Prerequisites

- Node.js `>=24 <25` (repo pins `24` in `.nvmrc`; `node --version` should
  print `v24.x`)
- npm (ships with Node)
- Python 3 (only needed for `npm run validate:repository`)

## 2. Install

```sh
git clone <repo-url> QA-Intelligence
cd QA-Intelligence
npm install
npm run build          # compiles src/ -> dist/, required before any MCP entrypoint runs
```

Optional sanity checks:

```sh
npm run typecheck
npm test               # builds then runs dist/tests/**/*.test.js
npm run validate       # repository governance + schemas + typecheck + test + audit
```

## 3. Usage — two ways to install (per host)

Two independent install methods. Pick one per host, or run both against
the same checkout:

- **Method A — local `stdio`** (`dev-entrypoint.ts`): host spawns
  `node dist/src/mcp/dev-entrypoint.js` itself as a child process. No
  network involved, fixture auth only. This is what `hosts/*` ships as
  `.example`/plugin config today.
- **Method B — remote URL** (`remote-dev-entrypoint.ts`): you start the
  server yourself (§5) as a long-running HTTP process, then point the host
  at its URL with a bearer token. Real signed OIDC tokens, but still
  dev-only membership (§5, §6).

Run `npm run build` first either way — both entrypoints launch compiled
JS, not TypeScript source.

### 3.1 Claude Code

**Method A (stdio)** — config lives at
`hosts/claude-code/.claude-plugin/plugin.json`, already relative to that
directory:

```json
{
  "mcpServers": {
    "qa-intelligence": {
      "command": "node",
      "args": ["../../../dist/src/mcp/dev-entrypoint.js"],
      "env": { "QA_INTELLIGENCE_DEV_WORKSPACE_ID": "workspace-claude-code-dev" }
    }
  }
}
```

Install as a Claude Code plugin from the repo (path form), then in a
Claude Code session confirm the server connected and the tool is visible
(e.g. `/mcp` or your host's server-list command) before calling it.

**Method B (URL)** — start the remote server first (§5), then add to
`.mcp.json` or `~/.claude.json`, or run `claude mcp add`:

```json
{
  "mcpServers": {
    "qa-intelligence-remote": {
      "type": "http",
      "url": "http://127.0.0.1:8787/mcp",
      "headers": {
        "Authorization": "Bearer <token printed by remote-dev-entrypoint.js on startup>"
      }
    }
  }
}
```

CLI equivalent:

```sh
claude mcp add --transport http qa-intelligence-remote http://127.0.0.1:8787/mcp \
  --header "Authorization: Bearer <token>"
```

`"type": "http"` is required for remote entries (stdio entries omit
`"type"` and use `"command"` instead).

### 3.2 Codex

**Method A (stdio)** — `hosts/codex/.codex-plugin/plugin.json` uses the
same relative path under a `mcp.servers` key instead of `mcpServers` —
install per Codex's plugin mechanism, same compiled entrypoint.

**Method B (URL)** — start the remote server first (§5), then in
`~/.codex/config.toml`, add a `url` key instead of `command` (Codex infers
transport from whichever key is present):

```toml
[mcp_servers.qa-intelligence-remote]
url = "http://127.0.0.1:8787/mcp"
bearer_token_env_var = "QA_INTELLIGENCE_DEMO_TOKEN"
```

Export the printed token into that env var before starting Codex:
`export QA_INTELLIGENCE_DEMO_TOKEN=<token printed by remote-dev-entrypoint.js>`.

### 3.3 Cursor

**Method A (stdio)** — copy `hosts/cursor/mcp.json.example` into your
Cursor MCP settings and replace the placeholder with an **absolute** path
(Cursor config is not relative to the repo):

```json
{
  "mcpServers": {
    "qa-intelligence": {
      "command": "node",
      "args": ["/absolute/path/to/QA-Intelligence/dist/src/mcp/dev-entrypoint.js"],
      "env": { "QA_INTELLIGENCE_DEV_WORKSPACE_ID": "workspace-cursor-dev" }
    }
  }
}
```

**Method B (URL)** — copy `hosts/cursor/mcp-remote.json.example` instead,
after starting the remote server (§5):

```json
{
  "mcpServers": {
    "qa-intelligence-remote": {
      "url": "http://127.0.0.1:8787/mcp",
      "headers": {
        "Authorization": "Bearer <paste the demo token printed by remote-dev-entrypoint.js on startup>"
      }
    }
  }
}
```

### 3.4 Manual smoke test (any host, or raw stdio)

You can run the server directly to confirm it starts:

```sh
QA_INTELLIGENCE_DEV_WORKSPACE_ID=workspace-manual-dev node dist/src/mcp/dev-entrypoint.js
```

It speaks MCP `2025-06-18` over stdio — it will sit waiting for JSON-RPC
frames on stdin. Use your host's tool-call UI rather than hand-writing
frames.

## 4. MCP tools (development catalog)

Authoritative list: `hosts/README.md` and `src/mcp/dev-fixture.ts`. Summary:

| Tool | Role |
|------|------|
| `assess_requirement_quality` | SPEC-203/202 requirement review (seed `REQ-DEMO-001`) |
| `discover_product_context` | SPEC-201 Knowledge Store discovery by objective |
| `discover_ui_surface` / `discover_ui_surface_after_login` | Live Semantic UI Map (+ login) |
| `generate_test_cases` / `execute_generated_test_case` | Design + flake-aware execute |
| `run_auto_qa` | Full Senior QA pipeline + HTML report + release gate + prior avoidance hints |
| `assess_ui_accessibility_smoke` | Naming a11y smoke (also inside `run_auto_qa`) |
| `generate_exploratory_charter` / `execute_exploratory_session` | Charter + multi-browser session |
| `assess_defect_quality` / `draft_defects_from_qa_run` | SPEC-211 assess / draft |
| `assess_*_quality` (BA/risk/strategy/TC/dataset/automation/report/execution_record) | Document quality assessors |
| `execute_api_smoke` | HTTP API smoke/contract |
| `run_depth_smokes` | a11y subset + perf + security heuristics |
| `register_workspace_secret` / `list_workspace_secrets` | Credential registry |
| `list_failure_avoidance_hints` | Session Memory avoid:* hints |
| `execute_browser_test` | **DEMO ONLY** seeded plans |

**Release posture:** `0.1.0-dev`. Production blocked on GOV-012 G2–G6.

### 4.1 Example — requirement review

Once the MCP server is connected, ask the host to call the tool, e.g.:

> Call `assess_requirement_quality` with `requirement_ref: "REQ-DEMO-001@1.0.0"`.

Expected shape of the result: an assessment record referencing
`REQ-DEMO-001`, findings from both rule engines
(`RequirementQualityRuleEngine`, `RequirementIntelligenceRuleEngine`), and
`resolved_versions` metadata (`agent`, `skill`, `prompt`, `rule_set`,
`knowledge_snapshot`, `policy`, `input_schema`, `output_schema`).

### 4.2 Example — Senior QA against a URL

> Use `/qa-intelligence:test` (or call `run_auto_qa`) with the target URL and
> acceptance criteria. Read `release_recommendation`, `draft_defects`, and
> `accessibility_smoke` before claiming the screen is ready.

### 4.3 Example — requirement not in the seed

```json
{ "requirement_ref": "REQ-NOT-REAL@1.0.0" }
```

Expect a resolution failure — `InMemoryRequirementResolver` only knows
`REQ-DEMO-001`. This is expected dev-seed behavior, not a bug.

### 4.3 Session memory

The registry keeps one `SessionMemory` per process, keyed by
`<toolName>:last_outcome`. A second call to `assess_requirement_quality`
in the same running server process can read the prior call's outcome —
this only holds within one running `stdio` process (one Workspace), not
across restarts.

## 5. Starting the remote server (required before Method B in §3)

For exercising the real signed-OIDC identity path instead of the fixture
verifier:

```sh
npm run build
node dist/src/mcp/remote-dev-entrypoint.js
```

- Listens on `http://127.0.0.1:8787/mcp` by default.
- Override with `QA_INTELLIGENCE_DEV_REMOTE_PORT` / `QA_INTELLIGENCE_DEV_REMOTE_HOST`.
- On startup it prints a real, signed demo bearer token to **stderr**. Keep
  this process running — it's what Method B in §3 connects to.
- Use the printed token in whichever host config from §3.1–§3.3 Method B
  you're setting up.

This is still non-production: the membership store is a single-actor
in-process fixture, and production enablement is blocked on GOV-012
G1–G4 regardless of transport.

### 5.1 Dev token vs. a real production token

The `headers.Authorization: Bearer <token>` field in Method B (§3) is the
correct shape for production too — that part doesn't change. What changes
is where the token comes from:

| | Dev (what runs today) | Production (not built) |
|---|---|---|
| Token issuer | `remote-dev-entrypoint.ts` itself — mints its own ephemeral RSA keypair and serves its own local JWKS | A real IdP, per ADR-014 (still unbuilt) |
| How you get it | Printed to stderr when you start `remote-dev-entrypoint.js` | Standard OAuth flow (client credentials / auth code) against that IdP |
| Membership check | Single-actor in-process fixture (`OidcWorkspaceContextIssuer` in dev mode) | Real governed membership store (ADR-014, unbuilt) |
| Config field | `headers.Authorization: Bearer <dev token>` | Same field, `headers.Authorization: Bearer <real access_token>` |

Do not point a real host at a production URL using the dev-printed token —
it only verifies against the dev entrypoint's own self-signed JWKS, which
only exists while that one process is running. There is no production
remote MCP endpoint or IdP integration in this repo yet; when ADR-014's
identity work and GOV-012 G1–G4 land, only the token source and `url`
change, not the config shape shown in §3.

## 6. What's deliberately not covered here

- **API**: `api/README.md` — "Not started." No REST/GraphQL surface exists.
- **UI**: no UI package exists in this repo. All interaction today is
  through an MCP host's chat/tool-call interface as shown above.
- **Production auth (OIDC)**: ADR-014's production identity work is
  pending; both entrypoints above use dev-only fixture or self-signed
  identity, never a real IdP.

## 7. Where to go next

- `README.md` — repository status and reading order (`specs/foundation/...`,
  `governance/ARCHITECTURE_PRINCIPLES.md`, `governance/READING_ORDER.md`).
- `hosts/README.md` — authoritative source for everything in §3–§5.
- `ROADMAP.md` — implementation sequence, incl. what unlocks a real API/UI.
- `governance/AGENT_SKILL_QUALITY_GATES.md` — gates a Skill must pass
  before this moves past development.
