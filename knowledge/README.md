# Governed Knowledge Instances

This directory is reserved for repository-managed Knowledge Object instances that have passed SPEC-102 and QG-08.

It is intentionally empty of authoritative objects during the draft specification phase. Examples belong in `examples/knowledge/`; runtime Workspace knowledge belongs in the governed Knowledge Store, not in source control by default.

